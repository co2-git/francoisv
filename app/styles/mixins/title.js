import * as fonts from '../vars/fonts';

const title = () => ({
  fontWeight: 'bold',
  fontFamily: fonts.family.title,
  fontSize: fonts.size.headerTitle,
  lineHeight: '2em',
  margin: 0
});

export default title;
